/** The "or" rule between two ways of doing the same thing. */
export function OrDivider() {
  return (
    <div className="mf-or" role="separator" aria-label="or">
      <span>or</span>
    </div>
  )
}
